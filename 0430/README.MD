관리자님, 제공해주신 쉘 스크립트(Shell Script) 자동화 및 연산 제어의 핵심을 일목요연하게 압축했습니다. 마찬가지로 작대기 없이 원클릭 복사 및 암기가 가능한 형태로 정리한 텍스트 버전입니다.쉘 스크립트(Shell Script) 기본 및 연산 핵심 요약1. 스크립트 구조 및 입출력Shebang (쉬뱅): 스크립트 최상단 첫 줄에 #!/bin/bash를 기입하여 실행할 인터프리터 환경을 명시함주석 및 다중 실행: # 기호로 코드 설명을 적으며, 명령어 사이에 세미콜론(;)을 쓰면 한 줄에서 연속 실행이 가능함메시지 출력: echo 명령어를 사용하며, -n 옵션을 주면 출력 후 줄 바꿈을 하지 않음사용자 입력: read -p "프롬프트문구" 변수명 형태로 입력을 받아 변수에 즉시 저장함2. 변수 제어 및 명령 치환시스템 환경 변수: $USER(현재 사용자), $UID(고유 ID), $HOME(홈 디렉터리), $PATH(명령어 검색 경로) 등사용자 정의 변수 규칙: 할당 시 변수명=값 형태로 대입하며, 등호(=) 앞뒤로 공백이 없어야 함. 값을 불러올 때는 이름 앞에 $를 붙임명령 치환 (Command Substitution): 명령어의 실행 결과 자체를 변수에 담는 기법이며, testing=$(date) 또는 백틱을 사용하여 testing=\date`` 형태로 작성함3. 산술 연산 (Math) 처리 기법expr 명령어: 정수 계산용 고전 방식이며, 연산자 앞뒤로 반드시 공백을 두어야 함 (예: \expr $var2 / $var1``)대괄호 방식 ($[]): 괄호 내부에서 변수 앞의 $ 기호를 생략할 수 있어 편리함기본 연산: var3=$[ $var1 + var2 ]중첩 연산: $var4 = var1 \times (var2 - var3)$ 공식 적용 시 var4=$[ $var1 * ($var2 - $var3) ] 형태로 작성bc 계산기: 쉘에서 부동 소수점(실수) 연산을 처리할 때 사용하며, 파이프(|)와 조합함 (예: echo "scale=4; $var1 / $var2" | bc ➔ 소수점 4자리 지정)4. 종료 상태 코드 (Exit Status)$? 변수: 모든 명령어는 실행 직후 성공/실패 여부를 나타내는 상태 코드(0~255)를 이 변수에 남김exit 명령어: 스크립트를 즉시 종료시키며, exit $var3 형태로 연산 결과를 외부 상태 코드로 반환할 수도 있음대표적인 상태 코드 값:0 : 명령 성공126 : 명령은 찾았으나 실행 권한 없음127 : 명령어를 찾을 수 없음 (Command not found)
