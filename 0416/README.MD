리눅스 inode는 파일의 권한, 크기, 소유자 같은 모든 메타데이터를 저장하고 관리하는 핵심 블록이야.
파일의 실제 내용은 블록 노드에 저장되는데, 파일 크기가 블록 1개의 용량을 초과하면 그 초과분만큼 블록 개수가 늘어나며 할당돼.
결과적으로 하나의 inode는 파일 정보와 함께, 이렇게 여러 개로 쪼개진 블록 노드들의 위치 주소를 포인터로 연결하여 통합 제어한다.
